# Setup for projects using JSPM and Karma

Also using mocha and chai, but these are easily replaceable.

Rename the string `replacemodulename` in all files to the desired module name, and run:

```
npm install
jspm install
``` 